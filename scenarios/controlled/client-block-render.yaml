schemaVersion: 1
id: client_block_render
description: Verifies the controlled block render path emits the expected semantic state.
side: client
mods: [loaderbridge_test_probe]
steps:
  - {id: start, action: start_instance, timeout: PT3M}
  - {id: block_render, action: assert_render, timeout: PT2M, parameters: {subject: "block/loaderbridge:fixture", equals: "visible"}}
  - {id: stop, action: shutdown, timeout: PT1M, parameters: {marker: "LOADERBRIDGE_CLIENT_STOPPED"}}
