plugins { id 'net.minecraftforge.gradle' version '6.0.54' }

minecraft { mappings channel: 'official', version: '1.21.1'; reobf = false }

repositories {
    exclusiveContent {
        forRepository { maven { url = 'https://libraries.minecraft.net/' } }
        filter { includeGroup 'org.lwjgl' }
    }
}

dependencies {
    minecraft 'net.minecraftforge:forge:1.21.1-52.1.0'
    implementation project(':bridge-api')
    implementation project(':fabric-api-base-bridge')
    implementation project(':fabric-api-lookup-api-v1-bridge')
}

tasks.named('jar') {
    manifest {
        attributes('Automatic-Module-Name': 'dev.loaderbridge.fabric.transfer.api.v1',
                'LICENSE': 'Apache-2.0',
                'Implementation-Title': 'LoaderBridge Fabric Transfer API v1 Bridge',
                'Implementation-Version': '5.4.4+7b3d111d19-loaderbridge.9')
    }
}
